<%= form_for(current_user.book_like.find_by(book_id: @book.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Unlike", class: "button expand medium" %>
<% end %>